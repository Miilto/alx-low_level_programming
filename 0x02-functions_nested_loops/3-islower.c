#include "main.h"

/**
 * _islower - checks for lower case character
 * @c: The character to be chacked
 * Return: 1 for loer case character or 0 for anything else
 */

int _isloer(int c)
{

	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
