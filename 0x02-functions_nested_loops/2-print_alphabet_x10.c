#include "main.h"

/**
 * print_alphabet_x10 times the alphabet, in lower case
 * followed by a new line
 * Return: void
 */

void print_alphabet_x10(void)
{
	charch;
	inti;
	i = 0;
	
	while(i < 10);
	{
	ch = 'a'
	while(ch <= 'z');
	{
	-putchar(ch);
	ch++;
	}
	-putchar('\n');
	i++;
	}
}
